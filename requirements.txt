# Core API (FastAPI backend)
fastapi>=0.110.0      # Tested and verified with 0.136.3
uvicorn>=0.28.0       # Tested and verified with 0.49.0
pydantic>=2.6.0       # Tested and verified with 2.13.4

# Database Persistence
sqlalchemy>=2.0.0     # Tested and verified with 2.0.50

# Data & Analytics (physics simulation and anomaly detection)
pandas>=2.2.0         # Tested and verified with 2.3.3
numpy>=1.26.0         # Tested and verified with 2.2.6
plotly>=5.18.0        # Tested and verified with 6.8.0

# Dashboard Interface
streamlit>=1.32.0     # Tested and verified with 1.58.0

# Utilities
requests>=2.31.0      # Tested and verified with 2.34.2
python-dotenv>=1.0.1  # Tested and verified with 1.2.2
